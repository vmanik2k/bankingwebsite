<%- include('partials/header') %>

<div class="jumbotron text-center">
  <div class="container">
    <p class="secret-text">Search customer to  transfer Money</p>
    <hr>
    <div class="container mt-5">    
      <div class="row">
        <div class="col-sm-8">
          <div class="card">
            <div class="card-body">
    
              <!-- Makes POST request to /login route -->
              <form action="/login" method="POST">
                <div class="form-group">
                  <label for="accountno">Account NO</label>
                  <select name="accountno" id ="accountno">
                  <% user.forEach(element => { %>
                   <option class="form-control"  value=<%=element.accountno%> > <%=element.accountno%></option>
                  <% }) %>
                </select>
                  <input type="number" class="form-control" name="accountno1"> 
                </div>
                <div class="form-group">
                  <label for="amount">Amount</label>
                  <input type="number" class="form-control" name="amount">
                </div>
                <button type="submit" class="btn btn-dark">Transfer</button>
              </form>
    
            </div>
          </div>
        </div>
    
  <!-- <br>  <a class="btn btn-light btn-lg" href="/logout" role="button">Log Out</a> -->
    <!-- <a class="btn btn-dark btn-lg" href="/submit" role="button">Submit a Secret</a> -->
  </div>
</div>

<%- include('partials/footer') %>
